class Kascript extends HTMLElement { // (1)

  connectedCallback() {
  	var version = this.getAttribute('version');
      var lang = this.getAttribute('lang');
      document.write("this is a kascript with version" + version + "<br>" + "--------------------------------------------------------------------------------------------------------—--------------------------------------------------------------------------------------------------" + "<br>");
     }
    }
    
 class Info extends HTMLElement {
 	
 connectedCallback() {
 	var name = this.getAttribute('name');
     var desc = this.getAttribute('description');
     var other = this.getAttribute('other');
     document.write('<span style="border: 2px solid black;">' + '<h1>' + name + '</h1><br><h2>' + desc + '<br>' + other + '</h2></span>');
    } 
   } 
 
 	
 	
 	


class Inport extends HTMLElement {
	connectedCallback() {
	var type = this.getAttribute('type');
	var src = this.getAttribute('src');
	if ( type ==" script") {
		document.write('script src="' + src + '"></script>');
		} else {
			
			} 
			} 
		} 
	customElements.define("info", Info);
	customElements.define("kascript", Kascript);
	customElements.define("info", Info);
		
		
	
